// const numberButtons__wrapper = document.querySelector('.number-buttons__wrapper');
// const actionButtons__wrapper = document.querySelector('.action-buttons__wrapper');
// const calculatorInput = document.getElementById('calculatorInput');

// const numberButtons = [1, 2, 3, 4, 5, 6, 7, 8, 9, 0, ',', '='];
// const actionButtons = ['/', '*', '-', '+'];

// const HTMLNumberButtons = numberButtons.map(el => {
//     return `<div><button class='number-btn'>${el}</button></div>`
// }).join('');

// numberButtons__wrapper.innerHTML = HTMLNumberButtons;

// const HTMLActionButtons = actionButtons.map(el => {
//     return `<div><button class='action-btn'>${el}</button></div>`
// }).join('');

// actionButtons__wrapper.innerHTML = HTMLActionButtons;

